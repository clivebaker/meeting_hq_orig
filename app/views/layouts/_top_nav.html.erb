<!--<ul>
  <li><%= link_to "Log Out", destroy_user_session_path, 'data-turbo-method': :delete, class: "dropdown-item" if current_user %></li>
  <li><%= link_to "Organisations", organisations_path if current_user %></li>
  <li><%= link_to "Organisation: #{@organisation.name}", organisation_path(@organisation) if @organisation and @organisation.id.present? %></li>
  <li><%= link_to "Meeting: #{@meeting.name}", organisation_meeting_path(@organisation, @meeting) if @meeting and @meeting.id.present? %></li>
</ul>
-->

			<nav class="navbar navbar-expand navbar-light navbar-bg">
				<a class="sidebar-toggle">
          <i class="hamburger align-self-center"></i>
        </a>


				<ul class="navbar-nav">
					<li class="nav-item px-2 dropdown">
				
					</li>
				</ul>

				<div class="navbar-collapse collapse">
					<ul class="navbar-nav navbar-align">
						<% if current_user %>
						<li class="nav-item dropdown">
							<a class="nav-icon dropdown-toggle d-inline-block d-sm-none" href="#" data-bs-toggle="dropdown">
                <i class="align-middle" data-feather="settings"></i>
							</a>
							<a class="nav-link dropdown-toggle d-none d-sm-inline-block" href="#" data-bs-toggle="dropdown">
                <span class="text-dark"><%= current_user.email %></span>
        			</a>
							<div class="dropdown-menu dropdown-menu-end">
								<%= link_to edit_user_registration_path, class: 'dropdown-item' do  %>
									<i class="fal fa-user"></i> Profile
								<% end %>
								<div class="dropdown-divider"></div>
								<%= button_to "Log Out", destroy_user_session_path, html: { data: { turbo: false} }, method: :delete, 'data-turbo-method': :delete, class: "dropdown-item" %>
							</div>
						</li>
						<% end %>

					</ul>
				</div>
			</nav>

