		<nav id="sidebar" class="sidebar">
			<div class="sidebar-content js-simplebar">
				<a class="sidebar-brand" href="/">
					<span class="align-middle me-3"><%= image_tag 'logo-left-nav.png', height: 160 %></span>
        </a>

				<ul class="sidebar-nav">
					<li class="sidebar-item">
						<a data-bs-target="#multi" data-bs-toggle="collapse" class="sidebar-link collapsed">
              <i class="fal fa-house-night"></i> <span class="align-middle">Business Unit</span>
            </a>
						<ul id="multi" class="sidebar-dropdown list-unstyled collapseW" data-bs-parent="#sidebar">
						<% current_user.business_units.each do |business_unit| %>
							<li class="sidebar-item">
								<a data-bs-target="#multi-<%=business_unit.id%>" data-bs-toggle="collapse" class="sidebar-link collapsed">
                  <%= business_unit.name %>
                </a>
								<ul id="multi-<%=business_unit.id%>" class="sidebar-dropdown list-unstyled <%= collapse_business_unit((@business_unit.id if @business_unit), business_unit.id) %>">
									<li class="sidebar-item <%= active((@business_unit.id if @business_unit), business_unit.id) if @meeting.blank? and params[:controller] == 'business_units'%>">
										<%= link_to 'Org Settings', business_unit_path(business_unit), class: "sidebar-link", 'data-bs-target': "#" %>
									</li>
									<li class="sidebar-item <%= active((@business_unit.id if @business_unit), business_unit.id) if @meeting.blank? and params[:controller] == 'slide_templates'%>">
										<%= link_to 'Slide Templates', business_unit_slide_templates_path(business_unit), class: "sidebar-link", 'data-bs-target': "#" %>
									</li>
									<li class="sidebar-item">
										<% debug = "" %>
                     <%= link_to "Meetings #{debug}", '#', 'data-bs-target': "#multi-#{business_unit.id}-1", 'data-bs-toggle': "collapse", class: "sidebar-link collapsed" %>
										
										<ul id="multi-<%=business_unit.id%>-1" class="sidebar-dropdown list-unstyled <%#= collapse_meeting((@business_unit.id if @business_unit), business_unit.id)  %>">
											<li class="sidebar-item <%= active((@business_unit.id if @business_unit), business_unit.id) if @meeting.blank? and ['meetings', 'agendas', 'meeting_actions'].include?(params[:controller])%>">
												<%= link_to 'Manage Meetings', business_unit_meetings_path(business_unit), class: "sidebar-link", 'data-bs-target': "#" %>
											</li>										
												<% business_unit.meetings.each do |meeting| %>
													<li class="sidebar-item <%= active((@meeting.id if @meeting), meeting.id)%> " >
														<%= link_to business_unit_meeting_path(business_unit, meeting), class: "sidebar-link" do %>
															<%= meeting.name %> 
														<% end %>
													</li>
													<!-- li class="sidebar-item <%= active((@meeting.id if @meeting), meeting.id)%> " >
														<%= link_to business_unit_meeting_path(business_unit, meeting), 'data-bs-target': "#multi-#{business_unit.id}-2-#{meeting.id}", 'data-bs-toggle': "collapse", class: "sidebar-link collapsed  #{active((@meeting.id if @meeting), meeting.id) == 'active' ? 'secondary-nav' : ''}" do %>
															<%= meeting.name %> 
														<% end %>
													</li>
														<ul id="multi-<%=business_unit.id%>-2-<%=meeting.id%>" class="sidebar-dropdown list-unstyled  <%= collapse_meeting((@meeting.id if @meeting), meeting.id, ['meetings', 'agendas', 'meeting_actions'])  %>">
															<li class="sidebar-item  <%= meeting_component_active((@meeting.id if @meeting), meeting.id, ['meetings'], ['show', 'edit']) %> <%#=  (['meetings'].include?(params[:controller]) and ['show'].include?(params[:action])) ? 'active' : ''%>">
																<%= link_to business_unit_meeting_path(business_unit, meeting), class: "sidebar-link" do %>Summary <% end %>
															</li>
															<li class="sidebar-item  <%= meeting_component_active((@meeting.id if @meeting), meeting.id, ['agendas'], ['index', 'new', 'show', 'edit']) %><%#=  (['agendas'].include?(params[:controller]) and ['index'].include?(params[:action])) ? 'active' : ''%>">
																<%= link_to 'Agenda', business_unit_meeting_agendas_path(business_unit, meeting), class: "sidebar-link", 'data-bs-target': "#" %>
															</li>
															<li class="sidebar-item <%= meeting_component_active((@meeting.id if @meeting), meeting.id, ['meeting_actions'], ['index', 'new', 'show', 'edit']) %> <%#=  (['meeting_actions'].include?(params[:controller]) and ['index'].include?(params[:action])) ? 'active' : ''%>">
																<%= link_to 'Actions', business_unit_meeting_meeting_actions_path(business_unit, meeting), class: "sidebar-link", 'data-bs-target': "#" %>
															</li>										
													</ul-->
												<% end %>

										</ul>
									</li>
								</ul>
							</li>
							<% end %>
						</ul>
					</li>


					<li class="sidebar-item ">
						<%= link_to business_units_path, class: "sidebar-link" do %>
							<i class="fal fa-house-night"></i> 
							<span class="align-middle">Manage Business units</span>
							<span class="badge badge-sidebar-primary"></span>
						<% end %>
					</li>

					<% if current_user.super_admin %>
						<li class="sidebar-item ">
							<%= link_to master_slide_templates_path, class: "sidebar-link" do %>
								<i class="fa-solid fa-brackets-curly"></i>
								<span class="align-middle">Master Slide Templates</span>
								<span class="badge badge-sidebar-primary"></span>
							<% end %>
						</li>

						<li class="sidebar-item ">
							<%= link_to components_path, class: "sidebar-link" do %>
								<i class="fa-solid fa-brackets-curly"></i>
								<span class="align-middle">Components</span>
								<span class="badge badge-sidebar-primary"></span>
							<% end %>
						</li>
					<% end %>

				</ul>

			</div>
		</nav>