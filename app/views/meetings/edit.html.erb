

<main class="content">
				<div class="container-fluid p-0">

				<div class="row">
						<div class="col-12">
							<div class="card">
								<div class="card-header">
									<div class="card-actions float-end">
										<div class="dropdown position-relative">
	
										</div>
									</div>
									<h5 class="card-title mb-0">Editing meeting for <%= @business_unit.name %></h5>
								</div>
								<div class="card-body">

                  <%= render "form", meeting: @meeting %>

                  <div class="row">
                    <div class="col">
                      <%= link_to "Show this meeting", business_unit_meeting_path(@business_unit, @meeting), class: 'btn btn-secondary'%>
                    </div>
                    <div class="col text-end">
                      <%= link_to "Back to meetings", business_unit_meetings_path(@business_unit), class: 'btn btn-secondary' %>
                    </div>
                  </div>

                </div>
							</div>
						</div>
					</div>

				</div>
			</main>

