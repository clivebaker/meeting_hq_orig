

<main class="content">
				<div class="container-fluid p-0">

				<div class="row">
						<div class="col-12">
							<div class="card">
								<div class="card-header">
									<div class="card-actions float-end">
										<div class="dropdown position-relative">
	
										</div>
									</div>
									<h5 class="card-title mb-0">New meeting for <%= @business_unit.name %></h5>
								</div>
								<div class="card-body">

                  <%= render "form", meeting: @meeting %>

                  <div class="row">
                    <div class="col">
                      
                    </div>
                    <div class="col text-end">
                      <%= link_to "Back", organisation_url(@organisation), class: 'btn btn-secondary' %>
                    </div>
                  </div>

                </div>
							</div>
						</div>
					</div>

				</div>
			</main>
