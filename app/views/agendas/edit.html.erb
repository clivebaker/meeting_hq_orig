

<main class="content">
				<div class="container-fluid p-0">

				<div class="row">
						<div class="col-12">
							<div class="card">
								<div class="card-header">
									<div class="card-actions float-end">
										<div class="dropdown position-relative">
	
										</div>
									</div>
									<h5 class="card-title mb-0">Editing agenda for meeting <%= @meeting.name %></h5>
								</div>
								<div class="card-body">

                  <%= render "form", agenda: @agenda %>

                  <div class="row">
                    <div class="col">
                      <%= link_to "Show this item", organisation_business_unit_meeting_agenda_path(@organisation, @business_unit, @meeting, @agenda), class: 'btn btn-secondary'%>
                    </div>
                    <div class="col text-end">
                      <%= link_to "Back to agenda",business_unit_meeting_agendas_path(@organisation, @business_unit, @meeting), class: 'btn btn-secondary' %>
                    </div>
                  </div>

                </div>
							</div>
						</div>
					</div>

				</div>
			</main>

