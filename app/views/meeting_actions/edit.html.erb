

<main class="content">
				<div class="container-fluid p-0">

				<div class="row">
						<div class="col-12">
							<div class="card">
								<div class="card-header">
									<div class="card-actions float-end">
										<div class="dropdown position-relative">
	
										</div>
									</div>
									<h5 class="card-title mb-0">Edit action for <%= @meeting.name %></h5>
								</div>
								<div class="card-body">

                  <%= render "form", meeting_action: @meeting_action %>

                  <div class="row">
                    <div class="col">
                      <%= link_to "Show this action", organisation_business_unit_meeting_meeting_action_path(@organisation, @business_unit, @meeting, @meeting_action) , class: 'btn btn-secondary' %>
                    </div>
                    <div class="col text-end">
                      <%= link_to "Back to actions", organisation_business_unit_meeting_meeting_actions_path(@organisation, @business_unit, @meeting) , class: 'btn btn-secondary' %>
                    </div>
                  </div>

                </div>
							</div>
						</div>
					</div>

				</div>
			</main>
