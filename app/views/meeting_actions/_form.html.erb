<%= form_with(model: [@organisation, @meeting, meeting_action]) do |form| %>
  <% if meeting_action.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(meeting_action.errors.count, "error") %> prohibited this meeting_action from being saved:</h2>

      <ul>
        <% meeting_action.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.hidden_field :meeting_id %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :note, style: "display: block" %>
    <%= form.text_area :note %>
  </div>


  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.select :user_id, options_for_select(@users.map{|e| [e.name, e.id]}, meeting_action.user_id), {prompt: 'Choose user'}, { class: ''} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
