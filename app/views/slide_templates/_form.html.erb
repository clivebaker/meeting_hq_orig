<%= form_with(model: [@organisation, slide_template]) do |form| %>
  <% if slide_template.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(slide_template.errors.count, "error") %> prohibited this slide_template from being saved:</h2>

      <ul>
        <% slide_template.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :organisation_id, style: "display: block" %>
    <%= form.text_field :organisation_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :enabled, style: "display: block" %>
    <%= form.check_box :enabled %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
